<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" />
    <link rel="stylesheet" href="index.css" />
  </head>

  <% links.forEach( (link) => { %>
    <video src="<%= link %>" autoplay="true" loop="true"></video>
  <% }) %>
</html>
